# 2026-08-23: plain PyPI only hosts CPU-only torch wheels for Windows/Linux --
# `pip install torch==2.8.0` with no index override silently gives you a
# torch that reports `torch.cuda.is_available() == False` even on a machine
# with a working GPU (confirmed: RTX 3070 Ti, driver 591.86, CUDA 13.1 --
# `nvidia-smi` fine, `torch.cuda.is_available()` False until reinstalled from
# this index). The RunPod pod image never hit this because its base image
# already had a CUDA torch baked in and `pip install -r requirements.txt`
# treats an installed `2.8.0+cu128` as satisfying a bare `torch==2.8.0`
# constraint (PEP 440 local-version matching), so it was never replaced
# there. Pinning the exact `+cu128` local version below plus this extra
# index closes that gap for every environment, baked-image or fresh.
--extra-index-url https://download.pytorch.org/whl/cu128

aiohappyeyeballs==2.6.1
aiohttp==3.13.2
aiosignal==1.4.0
annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.0
array-api-compat==1.15.0
array-api-extra==0.11.0
async-timeout==5.0.1
attrs==25.4.0
certifi==2025.11.12
charset-normalizer==3.4.4
click==8.1.8
contourpy==1.3.0
coverage==7.10.7
cycler==0.12.1
decorator==5.2.1
docstring-inheritance==2.2.2
edfio==0.4.13
EDFlib-Python==1.0.8
# 2026-08-25: DBConformer/SlimSeiz (Epilepsy/pipelines/dbconformer_classifier.py,
# slimseiz_classifier.py) -- pure-Python tensor rearrange/einsum helper the
# vendored model code uses (rearrange/einsum in DBConformer's attention
# blocks and SlimSeiz's MambaBlock). No CUDA/build-toolchain requirement,
# same "only depends on already-pinned torch" profile as mambapy above.
einops==0.8.2
exceptiongroup==1.3.1
fastapi==0.127.0
filelock==3.19.1
fonttools==4.60.2
frozenlist==1.8.0
fsspec==2025.10.0
gunicorn==23.0.0
h11==0.16.0
h5py==3.14.0
httptools==0.7.1
idna==3.11
importlib_resources==6.5.2
iniconfig==2.3.0
Jinja2==3.1.6
joblib==1.5.3
kiwisolver==1.4.7
lazy_loader==0.4
# 2026-08-24: dense_edge_temporal_mode="mamba" (Epilepsy/pipelines/
# cwt_gnn_classifiers.py's _DenseEdgeMambaTemporal). The upstream/official
# `mamba-ssm` package was evaluated first and rejected for THIS repo's
# environment (not for architectural reasons): PyPI ships mamba-ssm as an
# sdist only, no Windows wheel, and its selective-scan/causal-conv1d kernels
# are custom CUDA extensions requiring nvcc + a Linux toolchain to build --
# this repo runs on Windows (see the torch cu128 comment above) and must
# keep working on the author's MPS Mac too, which mamba-ssm's CUDA-only
# kernels cannot support at all. `mambapy` is a pure-PyTorch reimplementation
# of the same selective-SSM recurrence (parallel-scan, no custom kernels),
# so it runs identically on CUDA/MPS/CPU -- see the 2026-08-24 session note
# under Epilepsy/Session_notes/ for the full investigation. Only depends on
# torch (already pinned above); py3-none-any wheel, no platform-specific
# build step.
mambapy==1.2.0
MarkupSafe==3.0.3
matplotlib==3.9.4
memory-profiler==0.61.0
mne==1.12.1
mne-bids==0.19.0
-e git+https://github.com/noshore5/moabb.git@36e9ec8a7f26c39f737c651c3a983f31b690ba88#egg=moabb
mpmath==1.3.0
multidict==6.7.0
narwhals==2.24.0
networkx==3.2.1
nibabel==5.3.3
numpy==2.2.6
packaging==25.0
pandas==2.3.3
pillow==11.3.0
platformdirs==4.4.0
pluggy==1.6.0
pooch==1.8.2
propcache==0.4.1
psutil==7.2.0
pycountry==26.2.16
pydantic==2.12.5
pydantic_core==2.41.5
Pygments==2.20.0
pylsl==1.17.6
pyparsing==3.2.5
pyriemann==0.12
pytest==9.1.1
python-dateutil==2.9.0.post0
python-dotenv==1.2.1
pytz==2025.2
PyYAML==6.0.3
requests==2.32.5
scikit-learn==1.9.0
scipy==1.13.1
seaborn==0.12.2
six==1.17.0
starlette==0.49.3
sympy==1.14.0
threadpoolctl==3.6.0
torch==2.8.0+cu128
tqdm==4.67.1
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2025.3
urllib3==1.26.20
uvicorn==0.39.0
watchfiles==1.1.1
websockets==15.0.1
yarl==1.22.0
zipp==3.23.0
